---
- name: Install fabric.
  pip: >
    name=fabric
    state=present
  environment:
    PATH: "{{ fabric_python_bin_directory }}:{{ ansible_env.PATH }}"

- name: Copy tasks.py.
  copy: >
    src=fabfile.py
    dest={{ fabric_tasks_file_directory }}/fabfile.py
    mode=0644
    force=yes
  become: yes

- name: Change tasks.py owner.
  file: >
    path="{{ fabric_tasks_file_directory }}/tasks.py"
    state=file
    owner={{ fabric_user }}
    group={{ fabric_group }}
  become: yes