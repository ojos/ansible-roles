---
  django_python_bin_directory: ''
  django_repository: ""
  django_directory: /var/www
  django_project: app
  django_apps:
    - api
  django_user: "{{ ansible_user_uid }}"
  django_group: "{{ ansible_user_gid }}"
  django_install_packages:
    - 'Django'
    - 'mysqlclient'
    - 'django-redis'
    - 'pytz'
    - 'django-extensions'
    - 'django-requestlogging'
    - 'django-devserver'
    - 'guppy'
    - 'line-profiler'