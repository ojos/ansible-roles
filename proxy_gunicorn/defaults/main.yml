---
  proxy_gunicorn_user: "{{ openresty_user }}"
  proxy_gunicorn_group: "{{ openresty_user }}"
  proxy_gunicorn_bind: "{{ gunicorn_bind }}"
  proxy_gunicorn_nginx_sub_conf_directory: "{{ openresty_nginx_sub_conf_directory }}"
  proxy_gunicorn_nginx_client_max_body_size: "{{ openresty_nginx_client_max_body_size }}"
  proxy_gunicorn_static_dir: "{{ openresty_nginx_prefix }}/nginx/html"